# OpenAI API Key (required for AI generation)
OPENAI_API_KEY=sk-your-api-key-here

# Server Configuration
PORT=3001
NODE_ENV=development

# Admin API Key (for bypassing rate limits during development/testing)
# Generate a strong random key: openssl rand -hex 32
ADMIN_API_KEY=your-secret-admin-key-here

# Optional: Database connection (for future implementation)
# DATABASE_URL=postgresql://user:password@localhost:5432/qagent
